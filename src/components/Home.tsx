import React from 'react';
import Chips from './Chips';

const Home = () => {
  return (
    <div className='text-lg'>
      Home
      // display tags with chips - all + 5 more 
      <Chips />
    // import components - each one of the tags
    </div>
  )
}

export default Home;
